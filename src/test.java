import javax.swing.*;
import javax.swing.border.*;
import java.awt.*;//for color
public class test {
    public static void main(String [] args)
    {
        // JFrame frame=new JFrame();//create a frame
        // frame.setTitle("Jframe title goes here");//set title of the frame
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//when you hit x button will close and this is setting by default
        // frame.setResizable(false);
        // ImageIcon image = new ImageIcon("1.png");//create image icon
        // frame.setIconImage(image.getImage());//add it to my frame
        // frame.setSize(420,240);//set x-dimention and y-demantion
        // frame.setVisible(true);//make frame visible
        // frame.getContentPane().setBackground(new Color(0x123456));//set color of the background
        // Border border=BorderFactory.createLineBorder(Color.green,3);
        // JLabel label=new JLabel();//display an area for a string of text an image or both
        // label.setText("Hello Shahd");//set text of label
        // ImageIcon img=new ImageIcon("1.png");
        // // label.setIcon(img);
        // label.setHorizontalTextPosition(JLabel.CENTER);//set text left ,center,right of imgicon
        // label.setVerticalTextPosition(JLabel.BOTTOM);//set text top ,bottom ,center of the imgicon
        // label.setForeground(Color.GREEN);//set font color
        // label.setFont(new Font("MV Boil",Font.PLAIN,20));//set fon of text
        // label.setIconTextGap(100);//gap between text and icon if -ve so we ll' make them closer
        // label.setBackground(Color.black);
        // label.setOpaque(true);
        // label.setBorder(border);
        // label.setVerticalAlignment(JLabel.CENTER);//set verticsal position of the icons and text at the center within label
        // label.setHorizontalAlignment(JLabel.CENTER);
        //label.setBounds(0,0,250,250);//set x,y position within farme 

        // JLabel label=new JLabel();
        // label.setText("Hi");
        // label.setHorizontalAlignment(JLabel.CENTER);
        // label.setBounds(0,150,75,75);
        // JPanel redpanel=new JPanel();
        // redpanel.setLayout(new BorderLayout());
        
        // redpanel.setBackground(Color.red);
        // redpanel.setBounds(0,0,250,250);
        
        // JPanel bluepanel=new JPanel();
        
        // bluepanel.setBackground(Color.blue);
        // bluepanel.setBounds(250,0,250,250);
        
        // JPanel greenpanel=new JPanel();
        
        // greenpanel.setBackground(Color.green);
        // greenpanel.setBounds(0,250,500,250);
        

        

        MyFrame frame=new MyFrame();
        
        // frame.setLayout(null); //every thing at the frame become invisisbel
        // redpanel.add(label);
        // frame.add(redpanel);


        // frame.add(bluepanel);
        // frame.add(greenpanel);

        //the size of the frame become as the size of the components
    }
}
